jquery 
element -> do something with it
// CDN-- content delivery network

//$ - is not a reserved keywork in JS language
//$ - is a jQuery function -- this function returns an object with methods on the object

//jQuery lib link script
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>

// += -- adds to example 04-Stu_click-events
var password = "";
for (var i = 0; i < num; i++){
    password += getPasswordGen()
}


jQuery allows you to chain multiple methods. 

//navigating DOM example: 08-Stu_Dom
rootEL.children("ul").eq(0).children().eq(0).text("0")

//jQuery
 //creating a div element
  var displayLetterEl = $('<div>');

  //js vanilla
  var displayLetterEl = createElement()

//bootstrap link
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.0/css/bootstrap.min.css">

  //grid -- layout rules
  1. comumns must be immeadiate child of a row
  2. rows are only used to contain columns, nothing else
  3. rows should be placed inside a container
  4. containers -> rows -> columns